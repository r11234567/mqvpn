name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6:00 UTC

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Apply xquic patches
        shell: bash
        run: bash scripts/apply_xquic_patches.sh

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake libevent-dev

      # Cache keys carry a trailing -vN; bump in every job using the same
      # key whenever you change CMake flags or compiler. Keys here are
      # intentionally identical to the ci.yml `build` job so the two
      # workflows can share entries.
      - name: Compute xquic SHA
        id: xqsha
        run: echo "sha=$(git submodule status third_party/xquic | awk '{print $1}' | tr -d '+-')" >> "$GITHUB_OUTPUT"

      - name: Resolve BoringSSL pin
        id: bssl-pin
        # BoringSSL is a submodule of the xquic fork; its pin is the single
        # source of truth, so the cache key derives from it directly.
        run: echo "sha=$(git -C third_party/xquic/third_party/boringssl rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Cache BoringSSL (gcc fPIC)
        id: bssl-cache
        uses: actions/cache@v6
        with:
          path: third_party/xquic/third_party/boringssl
          key: bssl-${{ steps.bssl-pin.outputs.sha }}-linux-gcc-fpic-v2

      - name: Build BoringSSL
        if: steps.bssl-cache.outputs.cache-hit != 'true'
        run: |
          cd third_party/xquic/third_party/boringssl
          mkdir -p build && cd build
          # Must stay byte-identical to ci.yml's linux-build step: both jobs
          # share the bssl-*-linux-gcc-fpic-* cache key. See there re: why
          # CMAKE_BUILD_TYPE is mandatory.
          cmake -DBUILD_SHARED_LIBS=0 -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_C_FLAGS="-fPIC" -DCMAKE_CXX_FLAGS="-fPIC" ..
          make -j$(nproc) ssl crypto

      # Build xquic before CodeQL is initialized. It is an independently
      # maintained submodule and its findings belong in that repository;
      # mqvpn carries reviewed fixes through patches/xquic instead.
      - name: Cache xquic (Release)
        id: xquic-cache
        uses: actions/cache@v6
        with:
          # See ci.yml "Cache xquic (Release)" comment re: xqc_configure.h
          # — committed FEC=OFF default in source tree must travel with build/.
          path: |
            third_party/xquic/build
            third_party/xquic/include/xquic/xqc_configure.h
          # v4: bumped with the BoringSSL cache — libxquic.so absorbs it.
          key: xquic-${{ steps.xqsha.outputs.sha }}-linux-gcc-release-bssl-${{ steps.bssl-pin.outputs.sha }}-patch-${{ hashFiles('patches/xquic/*.patch') }}-v4

      - name: Build xquic dependency
        if: steps.xquic-cache.outputs.cache-hit != 'true'
        run: |
          cd third_party/xquic
          mkdir -p build && cd build
          cmake -DCMAKE_BUILD_TYPE=Release -DSSL_TYPE=boringssl \
                -DXQC_ENABLE_BBR2=ON \
                -DXQC_ENABLE_FEC=ON \
                -DXQC_ENABLE_XOR=ON \
                -DCMAKE_C_FLAGS="-Wno-dangling-pointer" ..
          make -j$(nproc)

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.37.3
        with:
          languages: c-cpp

      - name: Build mqvpn (CodeQL traced)
        run: |
          mkdir -p build && cd build
          cmake -DCMAKE_BUILD_TYPE=Release \
                -DBUILD_TESTING=OFF \
                -DXQUIC_BUILD_DIR=../third_party/xquic/build ..
          make -j$(nproc)

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4.37.3
        with:
          category: "/language:c-cpp"
